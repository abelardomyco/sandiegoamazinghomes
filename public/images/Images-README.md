# Images

Place site images here (e.g. logo, hero, agent photo).

- **Logo:** Optional `logo.png` or `logo.svg` for the header (currently text-only).
- **Hero:** Optional hero image for the homepage.
- **Agent / team:** Optional photo for About or contact section.

Reference paths in `content/_meta.json` if you add image metadata.
